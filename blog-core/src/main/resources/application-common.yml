spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:tcp://localhost:9086/mem:db;
      username: sa
      password:
      driver-class-name: org.h2.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        use_sql_comments: true

#logging.level:
#  org.hibernate.SQL: debug
#  org.hibernate.type: trace